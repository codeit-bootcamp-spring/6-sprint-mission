server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://discodeit_db:5432/discodeit
    username: discodeit
    password: discodeit
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.sprint.mission.discodeit: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

management:
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true