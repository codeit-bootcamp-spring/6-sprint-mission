server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://${RDS_ENDPOINT}/discodeit
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    properties:
      hibernate:
        ddl-auto: update
        format_sql: false

logging:
  level:
    com.sprint.mission.discodeit: info
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: warn

management:
  endpoint:
    health:
      show-details: never
  info:
    env:
      enabled: false